@model IEnumerable<Domain_Layer.DTO.TopTrendingsDTO>

<ul class="trends-list">
    @foreach (var trending in Model)
    {
        <li>
            <a href="@Url.Action("Hashtag", "Home", new { name = trending.Name })" class="card-link">
                <span class="trend-name">@trending.Name</span>
                <div class="trend-stats">@trending.UseCount posts</div>
            </a>
        </li>
    }    
</ul>
